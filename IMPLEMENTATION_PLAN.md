# Implementation plan

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Перевести канонічні пофарбовані `lesson-6/listings.html` і `lesson-6/profile.html` на UI kit: підключити `ui/kit.css`, замінити їхні вбудовані/локальні component styles на kit-класи; зберегти pixel-identical вигляд, а всі parity-виправлення робити лише в kit, не через screen-specific підгонку | in progress | `/root/migrate_first_kit_screens` | pending: route→source resolution, pre-change visual/computed baselines, kit class migration, pixel/computed parity, responsive/a11y/detector/crawler QA |
| Побудувати UI kit через Impeccable `extract` з усіх пофарбованих продуктових екранів разом зі state pages: плаский `ui/kit.css` (`:root` + component classes), markup-shell `ui/shell.html` (двошарова шапка + tab bar), повна state-вітрина `ui/kit.html` з текстами з `microcopy.md`; додати kit до головної навігації та передати перевірене HTTP-посилання | done | `/root/colored_ui_kit` | 17/17 colored pages; 35 `:root` tokens + 20 patterns/states; flat CSS 0 `@import`/`@layer`/shadows; semantic 2-layer shell + tabbar; microcopy/source identity checks; 320/390/430/1440 static layout + AA contrast; detector once (degraded, no kit finding); localhost and `192.168.31.21` exact kit/CSS/shell routes HTTP 200 with correct title; nav link + assets + `git diff --check` pass; no external LAN host/browser binary available |
| Прочитати всі `research/wireframes/*.html` разом із state pages та `sitemap.md`; створити `ui/inventory.md` як повний продуктовий інвентар реальних UI-компонентів, повторювані на 2+ екранах згрупувати за ролями (навігація, картки й списки, форми, зворотний зв'язок, чат) з екранами/станами/потребою у фото, а одиничні патерни винести в «Разове» | done | `/root/full_ui_inventory` | 62/62 HTML прочитано (60 product states + 2 service), 15/15 sitemap families; 5/5 role groups, 32 reusable rows, 5 one-offs; exact columns, required patterns, explicit refs and threshold evidence validated; `git diff --check` pass |
| Провести Impeccable `document` по пофарбованих продуктових екранах разом зі сторінками їхніх станів; згенерувати кореневий `DESIGN.md` і sidecar з коду макетів; синхронізувати кореневий `concept.md` з фактичними макетами, явно підписавши причину кожного атрибута; `DESIGN-artifacts.md` не змінювати | done | `/root/product_design_documentation` | 7 продуктових сімейств / 17 state pages; `DESIGN.md` 8/8 canonical sections; sidecar schema v2 / 6 components; 22 concept attribute/reason rows; JSON/structure/scoping/token consistency + `git diff --check` pass; `DESIGN-artifacts.md` absent and untouched, `beginners/source/concept.md` untouched |
| Зелений канон «Зелений двір»: `#365c4f`, `#4f7969`, Golos Text + Onest, без фіолетових ознак | done | `/root/green_mobile_lesson6` | `beginners/source/concept.html`, `concept.md`, `prototype/_base.css`; scan фіолетових lesson HTML/CSS tokens: 0 |
| Mobile-first композиція для 390 px; desktop як акуратне розширення/службова оболонка | done | `/root/green_mobile_lesson6` | `.impeccable/review/qa-mobile-*.png`, `qa-desktop-listings-empty.png` |
| Єдина дизайн-система на всіх 13 сторінках станів уроку 6 | done | `/root/green_mobile_lesson6` | `beginners/source/prototype/*.html` (13/13), спільний `_base.css` |
| Продуктова навігація «Пошук / Чати / Оголошення / Профіль / Додати» на всіх 13 сторінках | done | `/root/green_mobile_lesson6` | 13/13 navigation blocks; 5 links, 1 active; mobile target 72.8×70 px; Solar assets |
| Спільне навчальне дерево 01–06 на root, research і lesson 6; mobile drawer + desktop sidebar | done | `/root/green_mobile_lesson6` | `course-nav.css/js`; 19 HTML includes; `qa-mobile-course-open.png`, `qa-desktop-course-research.png` |
| Усі файлові правки виконують агенти; root лише координує і перевіряє | done | `/root/green_mobile_lesson6` | `AGENTS.md` |
| URL, screenshots, responsive та a11y перевірки | done | `/root/green_mobile_lesson6` | 13/13 lesson URLs і representative root/research/concept = HTTP 200; 0 horizontal overflow at 390; theme/focus/reduced-motion; Impeccable detector виконано (degraded parser; findings лише в incumbent root/research styles/copy) |

План є живим: кожна наступна користувацька правка спочатку фіксується тут, після чого root делегує її агенту й очікує статус.

## Lesson 6 UI redesign — current package

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Суттєво перезібрати UI всіх lesson-6 станів як цілісний mobile-first продукт для 390 px: виправити композицію, вертикальний ритм, ієрархію та зелений колірний канон; прибрати шаблонний «AI slop» вигляд | in progress | `/root/ui_redesign_completion` | shared `_base.css`; no-screenshot QA: 17 app pages × 4 viewports, 0 overflow/failures, canvas 320/390/390/390 |
| Усі основні CTA-кнопки розтягнути на повну доступну ширину; кнопки, меню/навігаційні контроли та однорядкові поля зробити повністю pill-rounded, не перетворюючи контентні картки й message bubbles на pills | in progress | `/root/ui_redesign_completion` | shared form/button/nav styles + DOM/layout QA |
| Спроєктувати відсутній екран «Подати заявку» як реальну форму з чітким recap, полями, validation/error/loading та окремим success state; route semantics і back-flow мають бути коректними | in progress | `/root/ui_redesign_completion` | `application*.html`, `application-sent.html`, course nav + link crawler |
| Замінити стандартні scrollbar на тонкий ненав'язливий системний стиль: прозорий track, напівпрозорий green thumb, hover-контраст, Firefox `thin`, WebKit ≈4 px; охопити body/device, filters, gallery і course sidebar/drawer | superseded | `/root/ui_redesign_completion` | Скасовано користувачем на користь повністю прихованих scrollbar |
| Приховати всі видимі scrollbar зі збереженням scrolling: `scrollbar-width:none`, WebKit scrollbar `display:none`/0 для html/body, device content, filters body, gallery, course shell/tree/drawer; не використовувати `overflow:hidden` на scrollable containers | in progress | `/root/ui_redesign_completion` | shared CSS + no-screenshot programmatic scrollTop/scrollLeft/computed overflow assertions |
| Додати узгоджені локальні raster/photo avatars замість ініціалів/emoji: список чатів, розмова, профіль, заявка та people surfaces оголошення; усі avatars строго круглі 1:1 (`aspect-ratio:1`, `border-radius:50%`, `object-fit:cover`), з intrinsic dimensions, коректним alt, стабільним crop і CLS на 390 px | in progress | `/root/ui_redesign_completion` | local avatar assets + lesson HTML/CSS; computed dimensions/radius/CLS metrics без screenshot capture |
| Прибрати sticky filters із верхньої частини всіх listings states; додати floating pill FAB «Фільтри» над product navbar і accessible bottom sheet з наявними controls, backdrop/close/handle, focus trap, initial focus, Escape/backdrop close, restore focus та робочими apply/reset; однакова семантика на success/empty/error/loading і в desktop device | in progress | `/root/ui_redesign_completion` | listings HTML + shared CSS/JS; 320/390/430/1440 keyboard/overflow QA |
| У filter drawer зробити видиму круглу 44×44 close button у top-right з Solar Duotone close icon та `aria-label="Закрити фільтри"`; зберегти Escape/backdrop close і restore focus | in progress | `/root/ui_redesign_completion` | `_base.css` + `course-nav.js`; DOM/computed/keyboard QA без screenshots |
| Прибрати всі тіні з lesson-6 app і shared course UI: `box-shadow`, `text-shadow`, `filter:drop-shadow`, shadow/elevation tokens у drawer/navbar/cards/buttons/composer/device/course shell; будувати ієрархію лише кольором, spacing і 1px borders | in progress | `/root/ui_redesign_completion` | source scan 0 declarations; computed scan 80/80 contexts = 0 shadows, без screenshots |
| Побудувати ієрархію локальних Solar icons: main nav/FAB/large states — Duotone; усі arrows — Solar Linear; small metadata/hints/facts/secondary actions — стримані Solar Linear 16 px лише де покращують scanning; drawer close — справжній Solar close cross, не arrow/text. Decorative icons `aria-hidden`, accessible names збережені | in progress | `/root/ui_redesign_completion` | local Solar Duotone + 16px Linear assets; CSS/HTML/computed-style scan без screenshot capture |
| Усі `.secondary` buttons/links зробити без border (`border:0` також у hover; focus зберігає лише accessible outline), відрізняти їх fill/text кольором | in progress | `/root/ui_redesign_completion` | shared `_base.css` + source/computed border scan без screenshots |
| Переробити trust footer усіх listing success/loading/relevant cards: trust proofs як компактні pills/chips у content, не full-width смуга; круглий real avatar 1:1 overlay на 3:2 photo з solid ring і без shadow; прибрати дату, актуальність показувати Solar Linear 16 px + короткий text label; зберегти trust-off/error semantics і loading geometry | in progress | `/root/ui_redesign_completion` | listings HTML/CSS + DOM/computed 320/390/430/1440 checks без screenshots |
| Прибрати contextual back link з root product tabs: усі listings family root states, `chats.html`, `profile.html`; title центрувати без резервного back slot. Solar Linear back лишити рівно один лише на nested `listing*`, `chat.html`, `compatibility*`, `application*`/sent | in progress | `/root/ui_redesign_completion` | HTML + appbar variant CSS; route/DOM counts без screenshots |
| Додати легкі two-color gradients лише з 2–3 tokenized palette pairs (dark forest→deep evergreen, pale mint→lime-cream) вибірково для profile hero, filter FAB/sheet header або status surfaces; не на кожній картці, без gradient text і без shadows, зі збереженням WCAG contrast | in progress | `/root/ui_redesign_completion` | shared CSS tokens + source/computed color/contrast QA без screenshots |
| Виправити collision stacked avatars із текстом у першому й третьому chat rows: виділити group avatar explicit grid column, constrain stack width, починати `.chat-copy` після неї; на 320 px адаптувати diameter/overlap без зміни single-avatar row | in progress | `/root/ui_redesign_completion` | automated no-screenshot gaps: 320=`10/10px`, 390=`12/12px`, 430=`12/12px`, 1440=`12/12px`; 0 failures |
| Повністю прибрати product top appbar/header з root tabs (`listings*`, `chats.html`, `profile.html`), не лишати 56px reserved gap; назву показувати один раз у content. Nested `listing*`, `chat`, `compatibility*`, `application*`/sent лишають рівно один appbar з title + Solar Linear back | in progress | `/root/ui_redesign_completion` | 80 no-screenshot DOM checks: root contextual appbar=0/content offset=0; nested appbar/back=1; 0 failures |
| Переробити desktop device shell (`min-width:431`) на впізнаваний iPhone-style без брендингу: neutral dark/titanium thin chassis, рівний чорний bezel, Dynamic Island, subtle side buttons і home indicator; строго без shadows. Inner canvas=390 px/≈844 px; на ≤430 chassis/bezel/island/buttons повністю відсутні й mobile edge-to-edge | superseded | `/root/ui_redesign_completion` | Скасовано користувачем: dark device simulation повністю прибирається |
| На desktop лишити тільки світлий 390px app canvas у простій neutral green-gray рамці 1px, radius 24–28px, padding 0–4px; без dark surround, island/buttons/home details і без shadows. Outside зберігає світлий/зелений course background; на ≤430 frame/radius/border відсутні | in progress | `/root/ui_redesign_completion` | shared CSS/runtime cleanup + no-screenshot computed background/pseudo/control/inner-width/mobile-frame QA |
| Замість blanket full-pill: усі ordinary text buttons/CTA/`a.btn`/form buttons та input/select/textarea/search fields мають computed radius 20px. `50%`/999px лишити тільки icon-only controls, filters FAB, avatars; compact trust/status chips і navigation dock/chips можуть лишатися pills | in progress | `/root/ui_redesign_completion` | centralized CSS + no-screenshot computed ordinary-control radius scan з explicit exceptions |
| Усі input/select/textarea/search fields lesson-6: default background точно `rgba(0,0,0,.05)`, `border:0`, radius20px; hover може `.07`, focus-visible лише outline, disabled/error — semantic neutral без persistent border; native appearance не має повертати border/background | in progress | `/root/ui_redesign_completion` | centralized field CSS + no-screenshot computed exact fill/border/radius assertions across representative fields |
| Filter drawer close показує лише Solar cross: transparent 44×44 hit target, без background/border/visible label; `aria-label` і focus-visible outline зберегти, general button styles явно reset | in progress | `/root/ui_redesign_completion` | close CSS/runtime + computed 44×44/background transparent/border0/mask/ARIA QA |
| Усі lesson-6 empty/error states зробити borderless/containerless: transparent background, no border/gradient/shadow, centered composition; Solar Duotone icon зверху → centered heading → centered recovery copy → full-width CTA radius20. Empty/error відрізняти лише semantic icon/text color, loading не змінювати | in progress | `/root/ui_redesign_completion` | shared state CSS/markup + no-screenshot DOM order/computed background/border/alignment/icon/CTA assertions |
| Прибрати stacked/group avatars із chat UI: кожен `.chat-row` і conversation identity має рівно один circular real avatar; для «Марія та Олег» стабільно використовувати primary contact Марію, груповість лишити в text name. Повернути single 52px column/gap без overlap | in progress | `/root/ui_redesign_completion` | chats/chat HTML/CSS + source/DOM exactly-one-image, 1:1/50% assertions без screenshots |
| Усі content cards зробити borderless при збереженні zero shadows: listing cards, application listing/recap, conversation context/composer, profile search blocks, state/card-like surfaces мають computed border-width 0; ієрархія через background/spacing. Виняток — outer desktop frame 1px і focus-visible outline | in progress | `/root/ui_redesign_completion` | centralized CSS + no-screenshot computed card border/shadow assertions |
| Filter FAB зробити icon-only: круглий 48×48, праворуч із 16px content gutter над bottom navbar/safe area без overlap; centered Solar Duotone tuning/filter ≈22px, без visible text/shadow, `aria-label="Відкрити фільтри"`, `aria-controls/expanded` збережені | in progress | `/root/ui_redesign_completion` | runtime/CSS + no-screenshot 48×48/50%/right-inset/text/accessibility/nav-overlap assertions на 320/390/430/1440 |
| Current refinement batch: neutral light frame, single chat avatars, centered containerless states, hidden scrollbars, borderless cards, exact 5% black fields, icon-only FAB/close, radius20 ordinary controls | in progress | `/root/ui_redesign_completion` | consolidated no-screenshot QA: 20 pages/80 checks/0 failures/0 captures; scroll behavior 4/4; crawler 6,323 refs/0 broken; syntax/diff/source scans clean |
| Empty/error state icon well: 64×64 circle with semantic pale mint/red fill and Solar Duotone icon; parent state remains transparent, borderless and centered | done | `/root/ui_redesign_completion` | targeted computed check at 390: empty `rgb(217, 233, 224)`, error `rgb(247, 224, 226)`; both 64×64/radius 50%, Solar Duotone mask; parent transparent/border 0/shadow none; `node --check` + `git diff --check` pass; no screenshots |
| Form affordances: suppress native select/date chrome; add vertically centered local Solar Linear 16px chevron/calendar with 16px right inset and protected text padding | done | `/root/ui_redesign_completion` | 320 computed: both icons 16px, center delta 0, right16/padding-right48, appearance none; date keeps native `showPicker()` click path; rgba black .05/border0/radius20; no screenshots |
| Compact UI batch: per-family state segments; remove product states from course tree; icon trust chips; 48px icon-only Add/send actions; plain filters fieldset; neutral unread chat with coral counter | done | `/root/ui_redesign_completion` | focused 320 DOM/computed: segments 44px/one current/overflow0; course states absent; chips 24px + Solar16/border0/shadow none; Add/send 48px/50%/icon-only; fieldset transparent/border0/options44; unread/read same bg + coral 20px counter; syntax/diff pass; no screenshots |
| Desktop design workspace + nav/trust refinement: family panel, external state switcher, borderless load-more, semantic Solar trust icons, transparent floating 3+Add nav, Linear plus, emerald gradient FAB | done | `/root/ui_redesign_completion` | focused DOM/computed: mobile panels/switcher hidden, width/overflow fixed; desktop 2 panels, 7 families/1 current, switcher external with 9px gap, screen390; nav outer transparent/border0/shadow none, first3 equal + Add48 Linear; trust clock/video/profile refs distinct16; load border0/shadow none; FAB two-color gradient/white icon; footer clearance token 98px vs nav76px = 22px; node/diff pass; no screenshots |
| Route correction: first course panel `04 · Вайрфрейми` opens canonical `lesson-6/listings.html` workspace and stays active across all lesson-6 routes; family panel keeps its own active route | done | `/root/ui_redesign_completion` | exact href `/kootok/lesson-6/listings.html`; programmatic click landed canonical URL; listing-error and destination both courseCurrent=1/familyCurrent=1; node/diff pass; no screenshots |
| Urgent artifact correction: canonical wireframes overview with two panels/7 direct families; semantic `voice.html`/`microcopy.html`; 60px equal-height bottom nav; temporarily remove hover-only effects | done | `/root/ui_redesign_completion` | overview: exact canonical href, 2 visible panels, 7 panel + 7 overview links, iframe0/overflow0, family click lands lesson-6; artifacts HTTP200, semantic h1/sections/tables, microcopy1139 inventory rows, one active, course `.md` href0; nav heights all60/Add width60/overflow0; project HTML/CSS `:hover` count0; node/diff pass; no screenshots |
| Final navbar refinement: light floating outer surface inset12/padding6/radius28 without border/shadow; Add remains 60×60 target but visually only transparent Solar Linear plus; active lime and content clearance retained | done | `/root/ui_redesign_completion` | 320/390 computed: inset12, outer rgba white .94/72px/padding6/radius28/border0/shadow none; all targets60 high, Add60×60/bg transparent/border0/radius0/Linear22/ARIA correct; active lime; footer clearance16px; overflow0; hover count0/diff pass; no screenshots |
| Revert only Add treatment: restore distinct circular 60×60 Add button with Solar Linear plus while retaining the light floating outer navbar | done | `/root/ui_redesign_completion` | computed 390: 60×60/radius50%, primary `rgb(36,87,68)` + white Linear plus, border0/shadow none/ARIA correct; outer rgba white .94 unchanged; diff/hover scan pass; no screenshots |
| Перевірити route semantics і всі локальні посилання, 390 px композицію/overflow, повноширинні CTA, pill-контроли та 320/390/430/1440 DOM/computed metrics; виконати syntax/diff checks, keyboard/ARIA, source scans і detector без створення screenshots | in progress | `/root/ui_redesign_completion` | no-screenshot QA: 20 pages/80 checks/0 failures/0 captures; LAN crawler 6,329 refs/0 broken; syntax/diff clean; detector degraded, 2 existing layout-transition warnings |
| Закріпити обов'язковий agent workflow: plan-first, agent-only edits, root waits for status and verifies QA before `done`; не губити накопичені вимоги | done | `/root/ui_redesign_completion` | `AGENTS.md` |

## UI composition refinement

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Перезібрати композицію на 390 px: ритм 8/16/24/32, чіткі групи, менше випадкових ліній і однакова щільність екранів | done | `/root` | shared `prototype/_base.css`, mobile screenshots |
| Посилити зелений канон без блідої mint/SaaS шаблонності; узгодити canvas, surfaces, active, text і elevation | done | `/root` | shared semantic tokens у `_base.css` |
| Перекомпонувати профіль, список чатів і розмову; прибрати порожній провал у чаті та перекриття composer нижнім navbar | done | `/root` | `profile.html`, `chats.html`, `chat.html`; QA 76 layout checks, failures 0 |

## Known limitations

- Impeccable detector працював у degraded regex mode через відсутні HTML/CSS parser modules; його incumbent findings у root/research зафіксовані як undercount, не як clean bill of health.
- Незалежний finish reviewer не завершився у відведене вікно передачі й був зупинений за вказівкою root; повторний reviewer не запускався. Візуальні докази перевірено агентом вручну по одному файлу.
- Фінальний Impeccable detector для web-app rebuild також працював у degraded regex mode: 16 warning-level findings, переважно incumbent root/research styles; 2 стосуються навмисної width/margin анімації desktop sidebar. CDP layout, contrast та keyboard checks виконані окремо й пройшли без помилок.

## Web-app shell rebuild

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Read-only crawl root, research і lesson-6: локальні `href`, `src`, `action`, CSS `url()`, 404, хибні relative paths і redirect loops у двох URL-контекстах | done | `/root/green_mobile_lesson6` | `scripts/audit-local-links.mjs`: 81 HTML, 99 route contexts, 6,229 checks на кожному з `127.0.0.1` і `192.168.31.21`; 0 broken, 0 redirects; виправлено research alias paths і `_template.html` target |
| Lesson 6 product canvas канонічно 390 px: на viewport ≤390 `width:100%`, `max-width:390px`, `min-height:100dvh`, без рамки/обрізання/overflow; на 430 px і desktop не розтягується, а центрується у web-app shell поруч із course tree; 390×844 — головний acceptance, 320×568 — graceful shrink | done | `/root/green_mobile_lesson6` | `scripts/qa-web-app.mjs`: 13/13 states — canvas 320/390/390/390 px на viewport 320/390/430/1440; max overflow 0; acceptance body/main 390 px та min-height 844 px |
| Спільний collapsible desktop sidebar та accessible mobile drawer/overlay з Escape, focus management, `aria-expanded`, state memory | done | `/root/green_mobile_lesson6` | `course-nav.css/js`; keyboard smoke підтвердив open/close ARIA, focus на close, Escape + focus restore, desktop collapse persistence після reload |
| Дерево 01–06 + 13 state pages, active route і collapsible groups; старі sidebar не дублюються | done | `/root/green_mobile_lesson6` | 64 DOM checks: рівно 1 shell, 1 active route, 0 legacy sidebars; 2 native collapsible groups |
| Окрема продуктова navigation всередині lesson-6 content | done | `/root/green_mobile_lesson6` | 13/13 state pages мають 5 product links; CDP acceptance: fixed nav 390×76 px |
| Route paths працюють під `/kootok/` та canonical directories без filesystem hardcode | done | `/root/green_mobile_lesson6` | Friendly + canonical contexts у crawler; 0/6,229 broken на local і LAN origins |
| Green «Зелений двір» лишається mobile-first visual canon | done | `/root/green_mobile_lesson6` | `site-theme.css`, `_base.css`, 12 screenshots root/research/4 lesson families × tree closed/open; representative images visually reviewed |
| Єдині shared color tokens для app shell/root/research/lesson6: green primary, green surfaces, neutral text/borders, role-bound cream accent, semantic success/error/info; без видимих purple/Notion-like shared surfaces | done | `/root/green_mobile_lesson6` | Shared tokens in `site-theme.css`; research hero purple glow/text overridden; tested contrast ratios 5.21:1–11.62:1 across text, muted, primary, accent and semantic roles |
| Фінальний crawl, 0 broken targets, 0 overflow, screenshots 6 surfaces open/closed, keyboard/ARIA smoke | done | `/root/green_mobile_lesson6` | `scripts/audit-local-links.mjs` + `scripts/qa-web-app.mjs`: 12 screenshots, 64 layout checks, 0 failures; keyboard/ARIA clean |
| Окремий commit без `figmosha2` та `.impeccable` | done | `/root/green_mobile_lesson6` | Production/docs/scripts staged explicitly; `.impeccable` screenshots and `figmosha2` excluded from commit |

## Lesson 6 desktop device shell

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Спільний стриманий device mockup навколо незмінного канонічного lesson-6 canvas 390 px на wider screens: корпус, bezel, speaker/dynamic-island detail, home indicator і м’яка тінь без дублювання у 13 HTML | done | `/root/green_mobile_lesson6` | Один runtime wrapper у `course-nav.js`, shared presentation у `prototype/_base.css`; 13 HTML не дублюють markup |
| На viewport ≤430 shell повністю відсутній: edge-to-edge 320/390 mobile, без рамки й horizontal overflow | done | `/root/green_mobile_lesson6` | 13/13 CDP: screen 320/390/390 px на 320/390/430; computed border/padding/radius = 0; max overflow 0 |
| На desktop device shell співіснує з course tree, не перекриває його; inner product canvas лишається 390 px | done | `/root/green_mobile_lesson6` | 13/13 CDP at 1440×900: shell 416 px, inner screen/main/nav 390 px; device left ≥ course tree right; 0 overlap/overflow |
| QA 13/13: inner canvas 320/390/390/390 на viewport 320/390/430/1440, 0 overflow; screenshots 390×844 і 1440×900 з tree open/closed; crawler 0 broken | done | `/root/green_mobile_lesson6` | `scripts/qa-web-app.mjs`: 64 layout checks, 14 screenshots, 0 failures; mobile and desktop tree open/closed visually reviewed; crawler 81 HTML/99 contexts/6,229 checks, 0 broken/redirects; detector 0 findings |
| Окремий commit без `figmosha2` та `.impeccable` | done | `/root/green_mobile_lesson6` | Production/docs/scripts staged explicitly; screenshots and `figmosha2` excluded |

## Lesson 6 vertical listing cards

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| У всіх відповідних lesson-6 states фото картки зверху на всю ширину; explicit horizontal `aspect-ratio: 3 / 2`, `object-fit: cover`, intrinsic dimensions/CLS; loading placeholder має ті самі computed width/height; контент вертикально нижче | done | `/root/fable` | `_base.css` + 5 semantic local images з intrinsic dimensions; CDP 390: success і loading media `358 × 238.66`, ratio `1.5`, media width = card width, body одразу нижче |
| Ієрархія без зміни copy/product truth: фото → тип/бейдж → назва → ціна → короткі факти → trust proof/актуальність; виразний, але стриманий green card, стабільні radius/shadow/spacing, stretched link/focus | done | `/root/fable` | 5/5 success cards: 16 px radius, один `--shadow-soft`, 18 px title, 20 px price, facts grid, green/error trust footer; stretched `listing.html` link і card-level focus ring |
| Loading skeleton повторює вертикальну геометрію та не показує фото; success має 5 cards, перевірені trust-off і довгі українські назви | done | `/root/fable` | `scripts/qa-listing-cards.mjs`: success 5 cards/5 images/trust-off; loading 4 cards/0 images/4 placeholders/`background-image:none`; heading overflow `false` на 320/390/430/1440 |
| Responsive QA 320/390/430/1440, device shell збережено, 0 overflow; card screenshots success+loading у 390 і desktop device | done | `/root/fable` | Card matrix 8/8, `failures:[]`; canvas 320/390/390/390, overflow 0, desktop device `416` + inner `390`; 4 screenshots visually reviewed in one pass, one bounded fix, one confirmation |
| Crawler/diff-check clean та окремий commit без `figmosha2`/`.impeccable` | done | `/root/fable` | Local + LAN crawler: 81 HTML, 99 contexts, 6,229 checks each, 0 broken/redirects; full QA 64/64 + 14 screenshots, 0 failures; detector degraded regex, 0 findings; explicit staging, hash in handoff |

## Lesson 6 prototype label cleanup

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Прибрати видимий `Навчальний прототип · дані демонстраційні` з усіх 13 lesson-6 states, видалити dead `.demo-label` CSS без порожнього gap; зберегти green canon, navigation, course tree, 390 canvas, device shell і cards | done | `/root/fable` | Source scan: 13/13 markup removed, label/CSS matches 0; LAN URLs 13/13 HTTP 200; `qa-web-app`: canvas 320/390/390/390, max overflow 0, 64 checks + 14 screenshots, `failures:[]`; focused diff: тільки 13 labels, 1 dead CSS rule і цей plan row |

## Lesson 6 compact product navigation

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Перебудувати product nav як compact floating bottom bar: 4 stable tabs `Пошук / Чати / Оголошення / Профіль` + visually distinct primary `Додати` в тому самому footprint; DOM/ARIA order = visual order, hrefs збережені, create має explicit accessible label | done | `/root` | Shared CSS; 13/13 create links мають `aria-label="Додати нове оголошення"`; порядок 4 tabs + create збережено |
| Mobile 320/390: зовнішні side/bottom margins, safe-area support, radius 14–18 px, рівні 5 columns без horizontal scroll, Solar icons, labels readable, кожна target ≥44 px; course drawer і contextual back links не змінювати | done | `/root` | 320 nav 296px із 12px side margins; 390 nav 366px; висота 66px; 5 рівних columns, min-height link 54px; max overflow 0 |
| Active state очевидний через icon + label weight + bounded shape, не лише колір; рівно один коректний `aria-current="page"` за family на всіх 13 states; keyboard focus visible | done | `/root` | Active має mint bounded shape + border + 700 weight; shared `:focus-visible`; `qa-web-app` activeCount=1 і productLinks=5 на всіх states |
| На 430/1440 nav лишається всередині 390 canvas/device bezel та вище home indicator; bounded mobile+device visual pass, crawler/diff-check, окремий commit без `figmosha2`/`.impeccable` | done | `/root` | 430 floating nav 366px; desktop nav 370px з 10px inset у 390px device screen; 64 layout checks, 0 overflow; QA screenshots generated |

## Lesson 6 app bar and navigation logic

| Вимога | Status | Owner | Evidence |
|---|---|---|---|
| Замінити великий page-title block на sticky top app bar 56px: Solar back icon зліва, коротка назва сторінки геометрично по центру, 44px target і правильний back-flow на всіх 13 states | done | `/root` | 13/13 headers нормалізовано; `Пошук / Оголошення / Сумісність / Заявка`; shared CSS grid `44px 1fr 44px`; `qa-web-app` 64/64 checks, overflow 0 |
| Прибрати хибний product tab `Оголошення`, який вів на конкретну чужу картку; лишити `Пошук / Чати / Профіль / Додати`, а detail-family прив'язати до active `Пошук` | done | `/root` | 13/13 мають 4 product links і рівно один `aria-current`; QA `productLinks=4`, `activeCount=1`; create має explicit accessible label |
